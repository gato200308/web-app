Coloca aquí tu archivo APK y cualquier captura de pantalla que quieras mostrar.

Nombre recomendado del APK: `app-relase.apk` (tal como está referenciado en `index.html`).

Para probar localmente copia el APK a esta carpeta y luego sirve la carpeta `portfolio` con Python u otro servidor.
